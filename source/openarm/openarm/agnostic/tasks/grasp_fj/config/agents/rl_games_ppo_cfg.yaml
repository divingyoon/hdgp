# grasp_fj (Track B · Fabrics 없음) rl_games PPO MLP 설정 — grasp_kp 사본(이름만 다르다) + DESIGN §6(value_bootstrap True · gamma 0.99)
# 관측: actor 135D / critic 159D (asymmetric), 액션: 22D (팔 관절 7D 증분+EMA + 시너지 15D)
# Asymmetric Actor-Critic (has_central_value: True), MLP [512, 256, 128]. num_envs 는 512 의 배수.
params:
  seed: 42

  env:
    clip_observations: 5.0
    clip_actions: 1.0

  algo:
    name: a2c_continuous

  model:
    name: continuous_a2c_logstd

  network:
    name: actor_critic
    separate: False
    space:
      continuous:
        mu_activation: None
        sigma_activation: None
        mu_init:
          name: default
        sigma_init:
          name: const_initializer
          val: 0
        fixed_sigma: True
    mlp:
      units: [512, 256, 128]
      activation: elu
      d2rl: False
      initializer:
        name: default
      regularizer:
        name: None

  load_checkpoint: False
  load_path: ''

  config:
    name: agn_grasp_fj
    env_name: rlgpu
    device: 'cuda:0'
    device_name: 'cuda:0'
    multi_gpu: False
    ppo: True
    mixed_precision: True
    normalize_input: True
    normalize_value: True
    # ★True 필수(DESIGN §6): time-out·max_goals truncation 이 잦다 — rl_games 래퍼의
    #   `time_outs` 로 V(s') 를 얹어야 목표 근처의 미래 보상 절벽이 생기지 않는다.
    value_bootstrap: True
    num_actors: -1
    reward_shaper:
      scale_value: 0.01
    normalize_advantage: True
    gamma: 0.99
    tau: 0.95
    learning_rate: 3e-4
    lr_schedule: linear
    schedule_type: standard
    kl_threshold: 0.013
    score_to_win: 100000000
    max_epochs: 10000
    save_best_after: 100
    save_frequency: 50
    print_stats: True
    grad_norm: 1.0
    entropy_coef: 0.002
    truncate_grads: True
    e_clip: 0.2
    horizon_length: 64
    # minibatch_size: num_envs × horizon_length 이 반드시 나누어 떨어져야 함
    #   128  envs: batch=   8192 → minibatch= 2048 (4 splits)
    #   2048 envs: batch= 131072 → minibatch=32768 (4 splits)  ← 학습 권장
    minibatch_size: 32768     # --num_envs 2048 기준. 128 envs 사용 시 2048로 변경
    mini_epochs: 4
    critic_coef: 4
    clip_value: True
    clip_actions: False
    bound_loss_type: bound   # ★rl_games 는 'regularisation'(영국식)/'bound' 만 인식 —
                             #   'regularization' 은 어느 분기에도 안 걸려 bounds loss 가
                             #   12,652 iter 내내 꺼져 있었다(로그 0.0 이 증거)
    bounds_loss_coef: 0.005

    # Asymmetric Actor-Critic: critic은 143D privileged obs 사용
    has_central_value: True
    central_value_config:
      normalize_input: True
      normalize_value: True
      minibatch_size: 32768     # actor와 동일하게 맞출 것
      mini_epochs: 4
      learning_rate: 3e-4
      lr_schedule: linear
      kl_threshold: 0.013
      clip_value: True
      e_clip: 0.2
      network:
        name: actor_critic
        central_value: True
        mlp:
          units: [512, 256, 128]
          activation: elu
          d2rl: False
          initializer:
            name: default
          regularizer:
            name: None
